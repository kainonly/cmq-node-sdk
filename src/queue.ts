import {Instance} from './instance';
import {CreateQueue} from './queue/create-queue';

export class Queue {
    constructor(private args: {
        instance: Instance
    }) {
    }

    createQueue() {

    }
}
